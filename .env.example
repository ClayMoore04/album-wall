# Supabase (get from your Supabase project settings)
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key

# Supabase Service Role Key — server-side only, for auth verification in serverless functions
# Get from Supabase dashboard > Settings > API > service_role key
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# Spotify (get from https://developer.spotify.com/dashboard)
# Server-side only — for search API and OAuth token exchange
SPOTIFY_CLIENT_ID=your-spotify-client-id
SPOTIFY_CLIENT_SECRET=your-spotify-client-secret

# Spotify Client ID — browser-side, used for PKCE OAuth redirect
# Must match SPOTIFY_CLIENT_ID above
VITE_SPOTIFY_CLIENT_ID=your-spotify-client-id

# Resend (get from https://resend.com/api-keys)
# Server-side only — for sending email notifications
RESEND_API_KEY=re_your-resend-api-key

# One-time migration secret — set this, run the migration, then remove
# MIGRATION_SECRET=pick-a-temporary-secret
